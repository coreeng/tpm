Pods are intentionally replaceable, so applications should not depend on individual Pod IP addresses.

A Service provides a stable name and virtual IP for a group of matching Pods. Traffic can continue to flow through the Service as Pods scale, restart, or move between nodes.
