<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{{.Title}}</title>
<style>
:root { color-scheme: light; font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; color: #202124; background: #f6f7f9; }
body { margin: 0; }
main { max-width: 1120px; margin: 0 auto; padding: 32px 20px; }
header { margin-bottom: 24px; }
h1 { font-size: 32px; line-height: 1.15; margin: 0; }
h2 { font-size: 20px; margin: 0; }
p { line-height: 1.55; }
.heading-row { display: flex; align-items: center; flex-wrap: wrap; gap: 8px; margin-bottom: 8px; }
.source-badge { display: inline-flex; align-items: center; max-width: 100%; border: 1px solid #c6ccd6; border-radius: 999px; background: #f8fafc; color: #5f6368; padding: 2px 8px; font-size: 11px; font-weight: 700; line-height: 1.45; white-space: nowrap; }
.source-line { display: flex; align-items: center; flex-wrap: wrap; gap: 8px; color: #5f6368; font-size: 12px; font-weight: 700; margin-bottom: 6px; text-transform: uppercase; }
.field { margin-top: 12px; }
.meta { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 12px; }
.meta span { border: 1px solid #d8dde6; border-radius: 6px; background: #fff; padding: 6px 8px; font-size: 13px; }
.layout { display: grid; grid-template-columns: 320px 1fr; gap: 20px; align-items: start; }
.panel { background: #fff; border: 1px solid #d8dde6; border-radius: 8px; padding: 18px; }
.challenge-list { display: grid; gap: 8px; }
button { width: 100%; text-align: left; border: 1px solid #c6ccd6; background: #fff; border-radius: 6px; padding: 10px 12px; cursor: pointer; font: inherit; }
button:hover, button.active { border-color: #1a73e8; background: #eef4ff; }
.goal { border-top: 1px solid #e4e7ec; padding: 12px 0; }
.goal:first-child { border-top: 0; }
.muted { color: #5f6368; display: flex; align-items: center; flex-wrap: wrap; gap: 8px; }
.hidden { display: none; }
pre { white-space: pre-wrap; font: inherit; margin: 0; }
@media (max-width: 760px) { .layout { grid-template-columns: 1fr; } main { padding: 20px 14px; } }
</style>
</head>
<body>
<main>
<header>
<div class="heading-row"><h1>{{.Title}}</h1>{{if .Source}}<span class="source-badge" title="Source file">{{.Source}}</span>{{end}}</div>
{{if .TimeLimit}}<div class="muted"><span>{{.TimeLimit}}</span>{{if .TimeLimitSource}}<span class="source-badge" title="Source file">{{.TimeLimitSource}}</span>{{end}}</div>{{end}}
{{if .Description}}<div class="field"><div class="source-line"><span>Description</span>{{if .DescriptionSource}}<span class="source-badge" title="Source file">{{.DescriptionSource}}</span>{{end}}</div><pre>{{.Description}}</pre></div>{{end}}
{{if .State}}<div class="meta">
<span>Run {{.State.RunID}}</span>
<span>System {{.State.SystemNamespace}}</span>
<span>Workspace {{.State.WorkspaceNamespace}}</span>
<span>Registry {{.State.RegistryURL}}</span>
</div>{{end}}
</header>
<section class="layout">
<nav class="panel">
<h2>Challenges</h2>
<div class="challenge-list">
{{range $i, $challenge := .Challenges}}
<button type="button" data-target="challenge-{{$i}}"{{if eq $i 0}} class="active"{{end}}>{{$challenge.Title}}</button>
{{end}}
</div>
</nav>
<article class="panel">
{{range $i, $challenge := .Challenges}}
<section id="challenge-{{$i}}" class="challenge-detail{{if ne $i 0}} hidden{{end}}">
<div class="heading-row"><h2>{{$challenge.Title}}</h2>{{if $challenge.Source}}<span class="source-badge" title="Source file">{{$challenge.Source}}</span>{{end}}</div>
{{if $challenge.Description}}<div class="field"><div class="source-line"><span>Description</span>{{if $challenge.DescriptionSource}}<span class="source-badge" title="Source file">{{$challenge.DescriptionSource}}</span>{{end}}</div><pre>{{$challenge.Description}}</pre></div>{{end}}
{{range $goalIndex, $goal := $challenge.Goals}}
<div class="goal">
<strong>{{add1 $goalIndex}}. {{$goal.Title}}</strong> {{if $goal.Source}}<span class="source-badge" title="Source file">{{$goal.Source}}</span>{{end}}
{{if $goal.Description}}<p>{{$goal.Description}}</p>{{end}}
</div>
{{end}}
{{if $challenge.SuccessMessage}}<div class="field"><div class="source-line"><span>Success message</span>{{if $challenge.SuccessMessageSource}}<span class="source-badge" title="Source file">{{$challenge.SuccessMessageSource}}</span>{{end}}</div><pre>{{$challenge.SuccessMessage}}</pre></div>{{end}}
</section>
{{end}}
</article>
</section>
</main>
<script>
const buttons = [...document.querySelectorAll('button[data-target]')];
const panels = [...document.querySelectorAll('.challenge-detail')];
function showChallenge(target) {
  buttons.forEach((button) => button.classList.toggle('active', button.dataset.target === target));
  panels.forEach((panel) => panel.classList.toggle('hidden', panel.id !== target));
}
buttons.forEach((button) => button.addEventListener('click', () => showChallenge(button.dataset.target)));
</script>
</body>
</html>
