Welcome to the **Hello Module** — a minimal example that demonstrates the structure of a
Training Platform module: a module with one chapter, one section, and a short quiz.

Use it as a starting point and as a reference for `tpm validate` and `tpm build`.
