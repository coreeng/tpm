In this section you learn what `tpm` is for and how a module is laid out on disk:

- A **module** has metadata (`module.yaml`) and a description.
- A module contains **chapters**, each with its own metadata and description.
- A chapter contains **sections** (the reading content) and optional **assessments**.

That's everything you need to know to read the rest of this example.
